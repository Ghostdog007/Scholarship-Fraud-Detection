# Exclude datasets folder as requested by the user
datasets/

# Exclude virtual environment
.venv/

# Exclude python cache and notebooks checkpoints
__pycache__/
*.pyc
.ipynb_checkpoints/
*.ipynb

# V2 Generated Data and Models
engineered_features_v2.csv
identity_graph.pt
synthetic_exposure_set.pt
v2_feature_schema.json
vae_v2_scores.csv
graph_v2_scores.csv
risk_scores_v2.csv
pseudo_labels_v2.json
explanation_cards_v2.json
evt_thresholds_v2.json
training_log.txt
*.pth
/models
/outputs

# Retired MLflow leftovers (MLflow removed — run history now in outputs/model_registry.json)
mlruns/
mlflow.db

# Editor / shell crash dumps
*.stackdump

# Ablation checkpoint backup scratch
.ablation_backup/

# Pre-commit cache
.pre-commit-cache/

# DVC cache (content-addressed, managed by DVC — not git)
/data/processed/

# Local reversibility backup of the 68-feature artifacts (large .pth/.csv)
backup_68feat/


# Local environment (DB credentials) — never commit
.env

# Uploaded/generated test datasets (large, reproducible via scripts/)
/data/uploads/

# Playwright MCP session artifacts (browser test screenshots/traces)
.playwright-mcp/
/card_*.png
