# Development and tooling dependencies — not installed on the production server.
# Install with: pip install -r requirements-dev.txt

ruff>=0.4.0
mypy>=1.10.0
pytest>=8.2.0
pytest-cov>=5.0.0
pre-commit>=3.7.0
dvc[s3]>=3.50.0
httpx2>=2.0.0            # FastAPI TestClient transport (gate tests)
